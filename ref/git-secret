# pre-prerequisite
# 1 - git
# 2 - gpg
# 3 - git-secret

git secret usage                            # is used to print all the available commands
git init                                    # Initialize the repository if you have not done it already
git secret init                             # Initialize the repository using git-secret
git secret tell email@example.com           # Add a user
git secret whoknows                         # Get a list of which users are attached on the repo
git secret add file.txt                     # Add a file on the encryption list
git secret hide                             # Encrypt files of the list
git secret remove file.txt                  # deletes files from .gitsecret/paths/mapping.cfg, so they won’t be encrypted or decrypted in the future
git secret removeperson email@example.com   # removes a user
git secret list                             # prints all the currently added tracked files from the .gitsecret/paths/mapping.cfg
git secret clean -v                         # deletes all the encrypted files
git secret changes                          # shows changes between the current version of hidden files and the ones already committed

# To decrypt the file
# Make sure you have the secret key
git secret reveal                           # Decrypts the file
git secret cat                              # Outputs to stdout the contents of the files named on the command line
